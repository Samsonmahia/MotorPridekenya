{
  "name": "motorpridekenya",
  "version": "1.0.0",
  "description": "MotorPride Kenya - Premium Vehicle Marketplace",
  "type": "commonjs",
  "scripts": {
    "generate-index": "node scripts/generateCarsIndex.js",
    "build": "npm run generate-index && echo '✅ Build complete - cars index generated'",
    "dev": "npm run generate-index && npx live-server --port=3000",
    "prebuild": "npm run generate-index",
    "postinstall": "npm run generate-index",
    "test": "node scripts/test-car-files.js",
    "setup": "npm run generate-index && echo '✅ Setup complete!'",
    "emergency-build": "node scripts/generateCarsIndex.js && echo '🚨 Emergency build complete'"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": [
    "cars",
    "kenya",
    "marketplace",
    "vehicle",
    "automotive"
  ],
  "author": "MotorPride Kenya",
  "license": "MIT"
}
