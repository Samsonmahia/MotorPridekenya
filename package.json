{
  "name": "motorpride-kenya",
  "version": "1.0.0",
  "description": "MotorPride Kenya - Premium Vehicle Marketplace",
  "scripts": {
    "build": "node scripts/buildall.js",
    "build-cars": "node scripts/generatecarsindex.js",
    "build-contacts": "node scripts/generatecontactsindex.js",
    "dev": "npm run build && live-server --port=3000",
    "start": "npm run build && live-server --port=3000"
  },
  "keywords": ["cars", "marketplace", "kenya", "netlify-cms", "decap-cms"],
  "author": "MotorPride Kenya",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}